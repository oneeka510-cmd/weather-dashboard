version: "3.9"

services:
  weather-dashboard:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app        # live reload during dev
    restart: unless-stopped